import React from "react";
import Container from "../../components/container";

export const ClassWork = ({ navigation }) => {
  return <Container paddingStyle={{ alignItems: "center" }}></Container>;
};
